<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="/javascripts/color-modes.js"></script>
    <link href="/stylesheets/bootstrap.min.css" rel="stylesheet" />
    <link href="/stylesheets/bootstrap-icons.min.css" rel="stylesheet" />
    <script src="/javascripts/bootstrap.bundle.min.js"></script>
    <script src="/stylesheets/carousel.css"></script>

    <title>Workout</title>
  </head>
  <body>
    <% include ./partial/navbar %>
    <main>
      <div class="container m-5">
        <h2 class="text-center text-primary">Workouts</h2>
        <div class="row">
          <% for(var i = 0; i < workouts.length; i++){%>
          <div class="col-sm-12 col-md-6 col-lg-6">
            <div class="card mb-3" style="max-width: 540px">
              <div class="row g-0">
                <div class="col-md-6 p-2">
                  <img
                    src="/images/workout.jpeg"
                    class="rounded-start img-thumbnail"
                    alt="..."
                  />
                </div>
                <div class="col-md-6">
                  <div class="card-body">
                    <h5 class="card-title"><%= workouts[i].title%></h5>
                    <p class="fs-6 lh-sm">
                      Type :
                      <span class="fs-6 fw-light"><%= workouts[i].type %></span>
                    </p>
                    <p class="lh-sm fs-6">
                      Category : <%= workouts[i].category %>
                    </p>
                    <p class="lh-sm"><%= workouts[i].description %></p>
                    <a
                      href="/workout/<%= workouts[i]._id %>"
                      class="btn btn-outline-primary rounded-pill"
                      >View Challenges</a
                    >
                  </div>
                </div>
              </div>
            </div>
          </div>
          <%}%>
        </div>
      </div>
    </main>
    <br />
    <% include ./partial/footer %>
  </body>
</html>
